name: Production deep health monitor

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: production-deep-health
  cancel-in-progress: true

jobs:
  monitor:
    name: Verify production dependencies
    runs-on: ubuntu-latest
    timeout-minutes: 3
    env:
      HEALTHCHECK_URL: https://www.minerval.org/api/health?deep=1
      HEALTHCHECK_SECRET: ${{ secrets.MINERVAL_HEALTHCHECK_SECRET }}

    steps:
      - name: Require the monitoring secret
        run: test -n "$HEALTHCHECK_SECRET"

      - name: Check the application and its dependencies
        shell: bash
        run: |
          set -euo pipefail
          response_file="$(mktemp)"
          trap 'rm -f "$response_file"' EXIT

          http_status="$(curl \
            --silent \
            --show-error \
            --max-time 20 \
            --retry 2 \
            --retry-all-errors \
            --output "$response_file" \
            --write-out "%{http_code}" \
            --header "Authorization: Bearer $HEALTHCHECK_SECRET" \
            "$HEALTHCHECK_URL")"

          if [[ "$http_status" != "200" ]]; then
            echo "Production deep health check returned HTTP $http_status."
            cat "$response_file"
            exit 1
          fi

          jq -e '
            .service == "minerval" and
            .status == "ok" and
            (.checks | type == "object") and
            ([.checks[] | .ok] | all)
          ' "$response_file" >/dev/null || {
            echo "Production deep health response reported a degraded dependency."
            cat "$response_file"
            exit 1
          }

          echo "Production deep health check passed."

  browser:
    name: Verify production auth pages in a browser
    runs-on: ubuntu-latest
    timeout-minutes: 10
    # Independent of the health check on purpose: this catches breakage that is
    # invisible server-side, so it must still run when /api/health says "ok".
    env:
      PRODUCTION_URL: https://www.minerval.org

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Install browser runtime
        run: npx playwright install --with-deps chromium

      - name: Verify auth pages render a usable submit path
        run: npm run test:e2e:production
